import 'package:flutter/material.dart';

class BackendWidget extends StatelessWidget {
  const BackendWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Backend Screen"),
      ),
    );
  }
}
